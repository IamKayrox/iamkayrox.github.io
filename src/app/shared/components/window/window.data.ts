export interface WindowData {
    title?: string;
    noButtons: boolean;
    allowResize: boolean;
    initialAsMinimun: boolean;
    minimumWidth?: number;
    minimumHeight?: number;
    canClose: boolean;
    canMinimize: boolean;
}